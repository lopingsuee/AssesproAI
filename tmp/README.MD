# 📁 `tmp/` — Temporary Runtime Storage

This folder is **automatically used by the app at runtime** to store temporary files such as:

| Type | Description | Generated by |
|------|--------------|---------------|
| 🎥 `tmp/videos/` | Uploaded or downloaded video files | Candidate upload or video link |
| 🎧 `tmp/audio/` | Extracted audio in 16 kHz WAV format | Audio extractor (`extract_wav16k`) |
| 🗒️ `tmp/transcripts/` | JSON results and transcriptions | Evaluator and serializer |
| 📦 Others | Any intermediate data files | Internal runtime processes |

---

## ⚙️ Auto-generated Content

All contents in this folder are **created automatically** when you run the Streamlit app or backend pipeline.  
They are **not versioned in Git** (usually excluded via `.gitignore`).

You **do not need to create or modify anything manually** here.  
Just make sure the folder exists before running:

```bash
mkdir -p tmp/videos tmp/audio tmp/transcripts

---

## 🧹 Cleaning Up

You can safely delete this folder at any time to clear temporary data:

```bash
rm -rf tmp/*
```

The app will recreate it automatically on the next run.

---

## 🔒 Notes

* Do **not** upload or commit temporary files here to GitHub.
* The app handles file creation and cleanup automatically.
* If you are deploying this app (e.g., Streamlit Cloud or local server), ensure this folder is **writable** by the runtime process.

---

**In summary:**

> The `tmp/` directory is a working space used at runtime —
> for temporary storage of audio, video, and evaluation results generated automatically by the system.

```

---

💡 **Tips tambahan:**
Kalau kamu mau memastikan folder ini tidak kosong di GitHub (karena Git tidak menyimpan folder kosong), simpan file ini sebagai:

```

tmp/README.md

```

dan tambahkan ke `.gitignore` seperti ini agar hanya file README yang tersimpan:

```

tmp/*
!tmp/README.md

```

Setelah itu, kamu bisa commit dengan pesan misalnya:
```

git add tmp/README.md .gitignore
git commit -m "docs: add tmp/README explaining temporary runtime storage"

```

Mau saya bantu tuliskan juga `.gitignore` lengkapnya (biar project-mu clean saat di-push)?
```
